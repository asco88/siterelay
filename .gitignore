# Python
__pycache__/
*.py[cod]
*.pyo
.env

# Local env files — contain real secrets, never commit
.env.local
.env*.local

# OmniState runtime files — contain real tokens and live data, never commit
config.json
sensors.json
omni-state-style.json

# Node / Next.js
node_modules/
web/node_modules/
web/.next/
web/.env.local
web/.env*.local

# Vercel
web/.vercel/

# OS
.DS_Store
Thumbs.db
.vercel
